//viewer must have the ip address of the caster, and must have the possibility to see the screen sharing (create a methosd for watching a streaming video)
//for now ip address is a string, later will have to change
struct Viewer{
    name:String,
    username:String,
    ip_address:String
}

impl Viewer {
    //maybe it must change
    pub fn watch_stream(){

    }
    pub fn receive_stream(){

    }



}
